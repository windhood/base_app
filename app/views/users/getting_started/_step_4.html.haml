%h1
  = t('.set_up', :name => current_user.display_name)
  .description
    = t('.ready_to_share')
    %ul.inline_aspect_listing
      - for aspect in @aspects
        %li= aspect

%br
%br

%h3
  = link_to t('.continue'), root_path

%br
%br
%br

%p
  = t('.change_profile')
  = t('.user_menu')

%p
  = t('.manage_aspects')

.submit_block
  = link_to "#{t('.finish')} →", root_url, :class => "button"

