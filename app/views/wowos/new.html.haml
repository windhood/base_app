%hr/
%h1 #{t('.create_a_new_wowo')}
= render 'form'

= render 'component_templates'
= render 'settings_popup'
= render 'publish_popup'
= render 'published_popup'
- content_for :javascripts do
  = render 'page_javascript'
  